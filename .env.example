# 讯飞星辰 MaaS 平台 API 配置（Vercel 环境变量）
# 获取方式见 README「如何获取 API Key 和模型 ID」

# 必填：在模型详情页点【API调用】后，从"服务接口认证信息"复制
XFYUN_API_KEY=

# 选填：模型 ID，以你在模型卡片/API调用弹窗里看到的 modelId 为准
# （示例：xop35qwen2b）
XFYUN_MODEL=xop35qwen2b

# 选填：接口地址，一般无需修改
XFYUN_BASE_URL=https://maas-api.cn-huabei-1.xf-yun.com/v2

# 智谱（bigmodel.cn）优先：配置 ZHIPU_API_KEY 后分析走智谱，否则回退到讯飞
ZHIPU_API_KEY=
# 选填：模型 ID，默认 glm-4.6v-flash
ZHIPU_MODEL=glm-4.6v-flash
# 选填：思考开关，默认 disabled（快约 3 倍）；enabled 开启思维链（更慢但略深）
ZHIPU_THINKING=disabled
# 选填：接口地址，一般无需修改
ZHIPU_BASE_URL=https://open.bigmodel.cn/api/paas/v4

# 语义向量作者匹配（文风相似作者）——复用智谱凭证，无需额外密钥
# 与对话共用 ZHIPU_API_KEY；模型默认 embedding-3（2048 维）
# EMB_MODEL=embedding-3
# 重建向量库：node scripts/build-embedding-vectors.mjs（样本变动后必须重跑并提交 api/data/authorEmbeddings.js）
