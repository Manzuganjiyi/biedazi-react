# 工作方式

产品分析、方案、评审、迭代规划可按需通过 `skill` 工具加载 `product-manager` 技能（不再要求每次会话开始自动加载）。

# 版本号规则

- 格式：`X.YZ<字母>`，如 `1.00a`。
- 当前基准：从现在开始的每一次新部署以 `1.00a` 起。
- 每次代码修改（未部署）时，字母依次延后：`a → b → c …`。
- 每次实际部署，数字部分增加 `0.01`（如 `1.00a 部署后 → 1.01a`）。
- 发生你认为重大的改动时，先询问用户；若用户也认为重要，则数字部分增加 `0.1`（如 `1.00a → 1.10a`），否则按普通修改（字母延后）处理。
- 版本号用于 commit / 部署命名，不改 package.json 的 semver 字段（`1.00a` 非合法 semver）。

# 迭代报告自动追加

- 每次完成一个迭代（代码修改、commit 或部署）后，**必须自动追加迭代条目**到 `docs/迭代总结-原始.md`，不要等用户要求。
- 追加内容：
  - 在「迭代历程」列表追加对应版本条目（如 `1.00c — 一句话要点`）。
  - 如该迭代有完整工作细节，追加/更新「本次会话完整工作」小节。
  - 更新「贯穿主线的问题模式」中相关主线（新增或补充版本号）。
- 追加后用一句话告知用户已自动追加，不要额外索要。
